platform :ios, '9.0'

install! 'cocoapods', :warn_for_unused_master_specs_repo => false

target 'OneTimePasswordExample' do
  pod 'OneTimePassword', '~> 1.0'
  pod 'UAProgressView'

  target 'OneTimePasswordExampleTests' do
    inherit! :search_paths
  end
end
